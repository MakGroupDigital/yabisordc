export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';

export default function SiteTouristiqueLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

